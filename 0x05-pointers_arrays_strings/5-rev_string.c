#include "main.h"

/**
 * rev_string - function that reverses a string.
 *
 * @s: string
*/

void rev_string(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}

	for (i = i - 1; i >= 0; i--)
	{
	}
}
